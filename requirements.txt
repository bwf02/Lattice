# Evaluation dependencies.
# The venv inherits the host environment (torch/CUDA etc.) via `uv venv --system-site-packages`,
# so torch is NOT reinstalled here and the prebuilt CUDA build is preserved.
lm-eval>=0.4.11
# Model download backend: ModelScope is used instead of HuggingFace Hub so the
# pipeline works in environments where huggingface.co is unreachable.
modelscope>=1.10.0
